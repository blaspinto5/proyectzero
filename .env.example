# Example environment variables
# Copy to .env and edit before running if you want to use Postgres
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/projectzero
# For local SQLite fallback, leave DATABASE_URL unset and the app will use sqlite:///./projectzero.db
